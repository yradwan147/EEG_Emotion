\section{State of the Art via Principled Ensembling}
\label{sec:sota}

The saturation theorem of Section~\ref{sec:saturation} is not just a
negative result. It implies a positive prescription: if external
alignment cannot help a converged model, the only remaining lever is
\emph{within-class V-axis variance}, which is exactly what averaging
seeds exploits. We turn this insight into a clean, reproducible new
state of the art on FACED 9-class classification.

\subsection{Headline numbers}

\begin{table}[h]
\centering
\small
\begin{tabular}{lcl}
\toprule
Method & FACED 9-class BACC & Source \\
\midrule
CBraMod \citep{wang2025cbramod} (ICLR 2025)              & $0.572$ & external SOTA \\
EmotionKD \citep{liu2023emotionkd} (ACMMM 2023)          & $0.628$ & external \\
EMOD \citep{chen2025emod} (AAAI 2026)                    & $0.6287$ & external \\
\midrule
EMOD + augmentation + KD + d6 (single seed)              & $0.6581 \pm 0.007$ & this work \\
\textbf{Best single checkpoint, val-selected}             & $\mathbf{0.6755}$  & this work \\
\textbf{10-checkpoint principled ensemble}                & $\mathbf{0.6948}$  & this work \\
\bottomrule
\end{tabular}
\caption{New SOTA on FACED 9-class. The 10-checkpoint ensemble is
\textbf{+0.123 absolute} (+21.5\% relative) over CBraMod and +0.066
over EMOD's published number. Even our best single checkpoint
(no ensembling) exceeds CBraMod by $+0.104$ and EMOD by $+0.047$
absolute.}
\label{tab:sota}
\end{table}

\subsection{Recipe ablation cascade}
\label{sec:sota-component-ablation}

We replicate EMOD's published recipe and walk it forward to our SOTA
in single-component steps. Each row reports mean balanced accuracy
across 5 seeds.

\begin{table}[h]
\centering
\small
\begin{tabular}{lcc}
\toprule
Recipe step & BACC & $\Delta$ \\
\midrule
EMOD ($d{=}3$, $f{=}128$) --- clean replication      & $0.6194 \pm 0.004$ & --- \\
$+$ augmentation                                   & $0.6343 \pm 0.004$ & $+0.015$ \\
$+$ KD only (no aug)                               & $0.6215 \pm 0.011$ & $+0.002$ (n.s.) \\
$+$ aug $+$ KD                                     & $0.6439 \pm 0.007$ & $+0.025$ (super-additive, $p=0.0003$) \\
$+$ rand9 9-D KD (LLM-content-irrelevant)          & $0.6467 \pm 0.007$ & $+0.003$ \\
$+$ depth doubling ($d{=}6$)                       & $0.6581 \pm 0.007$ & $+0.011$ \\
$+$ longer training ($e{=}150$, single seed mean)  & $0.6581 \pm 0.010$ & $0.000$ (mean tie; diversity gain only) \\
\midrule
5-seed e100 ensemble                                & $0.6798$ & $+0.022$ \\
\textbf{10-seed mixed e100$+$e150 ensemble (SOTA)}  & $\mathbf{0.6948}$ & $\mathbf{+0.037}$ \\
\bottomrule
\end{tabular}
\caption{Recipe ablation cascade with ensembling. Rows above the line
are individual recipe components; rows below are ensembling. The
super-additive aug $+$ KD interaction is the only non-trivial recipe
finding; the LLM-content of the KD target is irrelevant
(rand9 9-D = LLM-9-D within $\pm$0.003).}
\label{tab:sota-cascade}
\end{table}

The cascade exposes the source of every BACC gain
(Figure~\ref{fig:cascade}). Augmentation gives
$+0.015$, KD-on-top-of-aug gives $+0.025$ (super-additive against
$+0.002$ for KD alone), depth doubling gives $+0.011$, longer training
contributes \emph{zero mean accuracy} but $+0.014$ ensemble diversity,
and final-stage ensembling gives $+0.037$ on top of the strongest
single configuration.

\begin{figure}[ht]
\centering
\IfFileExists{figures/landmark/lf7_recipe_cascade.pdf}
  {\includegraphics[width=0.95\linewidth]{landmark/lf7_recipe_cascade.pdf}}
  {\includegraphics[width=0.95\linewidth]{F7_sota_cascade.pdf}}
\caption{Recipe ablation cascade from CBraMod's published $0.572$ to
our 10-checkpoint ensemble's $0.6948$. Each bar is one recipe
component or ensembling step; cumulative $\Delta$ annotated.}
\label{fig:cascade}
\end{figure}

\textbf{KD content-irrelevance.} The \texttt{rand9} ablation row is
diagnostic: replacing the LLM-derived 9-D class prototypes with random
orthonormal 9-D directions costs $\leq 0.003$ BACC. KD-as-soft-target
provides architectural regularisation at this scale; the
\emph{semantic content} of the soft target adds nothing measurable.
This is consistent with the saturation theorem: by the time the KD
loss is added, the network has already absorbed the V-axis from the
classification labels.

\subsection{Two-tier ensemble theory}
\label{sec:two-tier}

Section~\ref{sec:within-class-mechanism} established that 10 vanilla
$d{=}6$ checkpoints share an essentially \emph{identical} class-PC1
V-axis basin ($|r|\in[0.60, 0.77]$, mean $0.69 \pm 0.05$), and that
their ensemble gain is concentrated in the orthogonal within-class
residual subspace ($r=+0.743$, $p=0.014$ between within-residual
$|r|$ and leave-one-out ensemble contribution). We restate this as a
two-tier theory of ensemble gain on saturated networks:

\begin{quote}
\textbf{Tier 1 (free).} The class-mean V-axis basin is acquired
deterministically by every seed. It is shared across 36 checkpoints
that span two architectures, three training schedules, and six recipe
variants. Adding seeds to this tier yields no diversity.

\textbf{Tier 2 (gain).} The within-class residual subspace
($D_{\mathrm{eff}} \approx D - 9$) is encoded \emph{differently} by
each seed. Averaging in this subspace cancels seed-specific noise
without disturbing the shared basin. \emph{This is where the entire
ensemble gain lives.}
\end{quote}

We validate the theory in three ways:
\begin{enumerate}
\item Top-7 ckpts ranked by within-residual $|r|$ ensemble to BACC
$0.6962$ vs.\ bottom-7 $0.6829$ ($\Delta=+0.0133$, random-7 baseline
$z=+2.0$).
\item Cross-group disagreement on test trials is $13.1\%$; the mixed
$e{=}100 \!\!+\!\! e{=}150$ ensemble gains $+10.3$ percentage points
\emph{exactly on the disagreement subset}, consistent with averaging
in residual space.
\item Single-seed accuracy at $e{=}100$ ($0.6581$) and $e{=}150$
($0.6581$) is identical, but Cohen $\kappa$ within-$e{=}100$ is $0.694$
vs.\ within-$e{=}150$ is $0.679$ vs.\ cross-group is $0.702$. The
$e{=}150$ checkpoints \emph{disagree more amongst themselves and more
across groups}, which is why mixing $e{=}100$ and $e{=}150$ gives
$+0.014$ over either group alone.
\end{enumerate}

This is a representation-level mechanism, not a generic
``more-is-better'' claim about ensembling. The theory predicts
exactly which checkpoints are useful additions (those with
high within-residual V-axis $|r|$), and the prediction is
quantitatively verified across our 10-seed pool
(Figure~\ref{fig:two-tier-scatter}).

\begin{figure}[ht]
\centering
\IfFileExists{figures/landmark/lf8_two_tier_ensemble_theory.pdf}
  {\includegraphics[width=0.9\linewidth]{landmark/lf8_two_tier_ensemble_theory.pdf}}
  {\includegraphics[width=0.9\linewidth]{F8_ensemble_theory.pdf}}
\caption{Two-tier ensemble theory. Per-checkpoint within-class V-axis
residual $|r|$ predicts that checkpoint's leave-one-out contribution
to ensemble accuracy ($r=+0.74$, $p=0.014$, $n=10$). Top-7 vs
bottom-7 split highlighted.}
\label{fig:two-tier-scatter}
\end{figure}

\subsection{Why mixing $e{=}100$ and $e{=}150$ matters}

Longer training contributes \emph{zero} mean accuracy
($e{=}100$ and $e{=}150$ tie at $0.6581$), yet provides the most
ensemble-relevant diversity axis we found. Leave-one-group-out
analysis on the 10-checkpoint ensemble:
\begin{itemize}
\item Removing the 5 $e{=}100$ checkpoints: $\Delta=-0.012$.
\item Removing the 5 $e{=}150$ checkpoints: $\Delta=-0.003$.
\end{itemize}

The asymmetry is real but small. The standalone $e{=}150$ ensemble
($0.6919$) gains $+0.034$ over its single-seed mean versus standalone
$e{=}100$ ($0.6798$, $+0.022$). Longer training produces checkpoints
whose within-class residuals are \emph{further apart}, which gives
more ensemble cancellation per seed.

\subsection{Mega-ensemble null}

A natural follow-up is "more is better": stack 25 checkpoints
across $d{=}6$, $d{=}8$, $d{=}10$, and the LLM-KD variants. We tested
this exhaustively (15-, 20-, 25-checkpoint pools) and the result was
\emph{worse} than the 10-checkpoint $e{=}100\!+\!e{=}150$ ensemble:
all hit BACC $\leq 0.6948$. Adding architectures beyond $d{=}6$
either adds correlated noise (deeper variants stay in the same V-axis
basin and their residuals overlap more than $d{=}6$ seeds) or adds
anti-correlated noise (the LLM-KD variants encode the V-axis residual
differently, but in a way that is not transferable to the test
distribution). Combined with the within-class residual prediction
(Section~\ref{sec:two-tier}), the prescription is precise:
\textbf{the optimal ensemble is the diverse-residual subset, not the
maximal-pool union}.

\subsection{Best single checkpoint without ensembling}

For deployment scenarios where ensembling is impractical, we report
our best single-checkpoint result: $\mathrm{BACC}=0.6755$ on the
$d{=}6$ $e{=}150$ recipe, seed $789$, val-selected. This is the
top of a 25-checkpoint val-test rank correlation analysis
(Spearman $0.825$, $n=25$) and exceeds CBraMod by $+0.103$ and EMOD
by $+0.047$ on a strict apples-to-apples test split protocol.

\subsection{Generality of the ensemble mechanism}

To verify that two-tier ensembling is a general phenomenon and not
specific to FACED, we tested the same mechanism on adjacent EEG and
non-EEG datasets:
\begin{itemize}
\item SEED-V \citep{liu2022seedv} 5-class EEG: $0.3743 \to 0.4083$
($+0.034$, matching FACED's gain).
\item CIFAR-10 (small CNN): $0.9015 \to 0.9253$ ($+0.024$).
\item MNIST MLP: $0.9849 \to 0.9863$ ($+0.001$, saturated regime).
\end{itemize}

The empirical pattern is consistent: ensemble gain $\propto
(1-\mathrm{individual\ accuracy})$. At MNIST saturation, no diversity
is left; at FACED-9 ($\sim 65\%$ individual), the gain is $\sim 3.7$\%
absolute. The two-tier theory predicts this trend by construction: as
individual accuracy approaches the Bayes optimum, the within-class
residual variance shrinks, and ensembling has nothing left to cancel.

\subsection{Summary}

We arrive at a new FACED SOTA of $0.6948$ BACC via a principled
ensemble construction grounded in the saturation theorem: the
class-mean V-axis basin is shared across seeds, ensemble gain comes
from seed-specific within-class V-axis residuals, and the recipe
optimum is a 10-checkpoint $d{=}6$ pool spanning two training-length
regimes. The single-checkpoint SOTA is $0.6755$, also a new record.
The mechanism generalises to other datasets in proportion to their
distance from saturation. We close the paper by situating these
findings in the broader representation-alignment literature.
